'use client';

import { ReferEarnPanel } from './ReferEarnPanel';

export default function ReferPage() {
  return (
    <div className="mx-auto max-w-2xl px-4 py-6">
      <ReferEarnPanel />
    </div>
  );
}
